import { ClockConfig } from '@/types';

export const CLOCK_CONFIG: ClockConfig = {
  RADIUS: 140,
  STROKE_WIDTH: 20,
  CENTER_RADIUS: 50,
  HOUR_MARK_LENGTH: 10,
  TOTAL_HOURS: 24,
  ANIMATION_DURATION: 300,
};
